import 'package:flutter/material.dart';

class AppIconsTheme {
  AppIconsTheme._();
  static const lightIconsTheme = IconThemeData(
    // Set the default color for icons in light mode
    color: Colors.black,
  );

  static const darkIconsTheme = IconThemeData(
    // Set the default color for icons in light mode
    color: Colors.white,
  );

}